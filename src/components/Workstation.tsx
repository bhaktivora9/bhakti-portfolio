interface WorkstationProps {
  isDark: boolean;
  toggleTheme: () => void;
}

const Workstation: React.FC<WorkstationProps> = () => {
  return (
    <div>
    </div>
  );
};

export default Workstation;